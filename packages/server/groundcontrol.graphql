type Workspace {
  id: ID!
  name: String!
  slug: String!
  projects: [Project!]
}

type Project {
  id: ID!
  name: String!
  repo: String!
  branch: String!
}

type Query {
  workspaces: [Workspace!]
}